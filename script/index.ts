/**
 * Takes a chain ID as an input, fetches data from https://chainid.network/chains.json, uses that
 * to read and infer various data and properties of the chain, and writes the result to `data/${chainId}.toml`.
 */