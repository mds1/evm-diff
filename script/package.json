{
	"name": "script",
	"module": "index.ts",
	"type": "module",
	"scripts": {
		"lint": "tsc --noEmit && bunx @biomejs/biome lint --apply .",
		"fmt": "bunx @biomejs/biome format --write .",
		"check": "bun lint && bun fmt",
		"main": "bash ./main.sh"
	},
	"devDependencies": {
		"@biomejs/biome": "1.6.4",
		"@types/bun": "^1.0.12"
	},
	"peerDependencies": {
		"typescript": "^5.0.0"
	},
	"dependencies": {
		"viem": "^2.9.16"
	}
}
